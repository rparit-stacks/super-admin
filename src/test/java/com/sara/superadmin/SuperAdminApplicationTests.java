package com.sara.superadmin;

import org.junit.jupiter.api.Test;

/**
 * Placeholder test. The full context test is intentionally omitted because it
 * requires a live MongoDB connection; add @SpringBootTest once a test Mongo
 * (e.g. embedded / testcontainers) is configured.
 */
class SuperAdminApplicationTests {

	@Test
	void contextLoadsPlaceholder() {
		// no-op
	}
}
